# =============================================================================
# DATABASE
# =============================================================================
# Default: SQLite (no config needed). For PostgreSQL:
# DATABASE_URL=postgresql://postgres:postgres@localhost:5433/personal_guru

# =============================================================================
# LLM CONFIGURATION
# =============================================================================
LLM_BASE_URL=http://localhost:11434/v1
LLM_MODEL_NAME=qwen3:30b
LLM_NUM_CTX=4096
LLM_API_KEY=na

# =============================================================================
# AUDIO PROVIDERS
# =============================================================================
# Options: 'openai' (Docker/Remote) or 'local' (Kokoro TTS / faster-whisper STT)
TTS_PROVIDER=openai
STT_PROVIDER=openai

# Docker/OpenAI URLs (only needed if using 'openai' provider)
TTS_BASE_URL=http://localhost:8969/v1
STT_BASE_URL=http://localhost:8969/v1

# Voice settings
TTS_VOICE_DEFAULT=af_bella
TTS_VOICE_PODCAST_HOST=am_michael
TTS_VOICE_PODCAST_GUEST=af_nicole

# STT Model (for Docker mode)
STT_MODEL=Systran/faster-whisper-medium.en

# =============================================================================
# APP SETTINGS
# =============================================================================
PORT=5011

# YouTube API Configuration
# Get your API key from: https://console.cloud.google.com/
YOUTUBE_API_KEY=YOUR_API_KEY
